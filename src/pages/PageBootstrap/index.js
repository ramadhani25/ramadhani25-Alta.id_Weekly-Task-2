import React from "react";
import Home from "../../components/Home";

const PageBootstrap = ({ data }) => {
  return (
    <>
      <Home data={data} />
    </>
  );
};

export default PageBootstrap;
