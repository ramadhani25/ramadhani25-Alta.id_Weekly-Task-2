import React from "react";
import Home from "../../components/Home";

const PageCleanCode = ({ data }) => {
  return (
    <>
      <Home data={data} />
    </>
  );
};

export default PageCleanCode;
